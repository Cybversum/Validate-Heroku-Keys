# Validate-Heroku-Keys

Get Heroku Keys from JS files


File with Heroku Keys -> heroku_key.txt
bash HerokuValidKey.sh | grep -v "unauthorized" | anew check.txt
rm only_keys.txt 

Go manual see if there are any valid
